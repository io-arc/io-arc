type: default

# working directory
wsDir:
  js: js

outputDir: dist

deployDir:
  js: [common, js]

options:
  js:
    minify: false
    splitFilename: assets
    sourceMap: true
    eslint: lint/.eslintrc.yml

  # use file-loader
  fileLoader:
    js:
      use: true
      hash: true
