extends assets/layouts/_base

block prepend consts
  -
    // Language key for 'assets/data/lang.yml'
    const $lang = 'en'

    // Page title
    const $title = SITE_TITLE

    // Page description
    const $description = SITE_DESCRIPTION

    // OGP settings
    const $ogp = {
      use: true, // insert flag (require)
      type: 'website',
      url: `${SITE_URL}/`,
      title: SITE_TITLE,
      image: `${SITE_URL}/ogp.png`
    }

block body
  p= SITE_TITLE
